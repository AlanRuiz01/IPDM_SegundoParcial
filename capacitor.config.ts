import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SegundoParcial_AlanRuiz',
  webDir: 'www'
};

export default config;
